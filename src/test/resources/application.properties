# テスト用設定。src/main/resources/application.properties は継承されず完全に置き換わるため、
# 必要な値はすべてここに書く。
spring.datasource.url=jdbc:sqlite::memory:
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.datasource.hikari.maximum-pool-size=1

gemini.api.key=test-dummy-key
gemini.api.base-url=https://example.invalid/v1beta/models
gemini.api.model=test-model
gemini.api.max-tokens=100
